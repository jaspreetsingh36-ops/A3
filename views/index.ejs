<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="bi bi-trophy-fill"></i> India Cricket Stats
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <% if (isAuthenticated && user) { %>
                    <li class="nav-item">
                        <a class="nav-link active" href="/">
                            <i class="bi bi-house"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/players">
                            <i class="bi bi-people"></i> Players
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/players/new">
                            <i class="bi bi-plus-circle"></i> Add Player
                        </a>
                    </li>
                <% } %>
            </ul>
            <div class="navbar-nav ms-auto">
                <% if (isAuthenticated && user) { %>
                    <div class="d-flex align-items-center">
                        <% if (user.avatar) { %>
                            <img src="<%= user.avatar %>" alt="Profile" class="rounded-circle me-2" width="32" height="32">
                        <% } else { %>
                            <i class="bi bi-person-circle me-2 text-light"></i>
                        <% } %>
                        <span class="text-light me-3"><%= user.displayName || user.name || 'User' %></span>
                        <a class="btn btn-outline-warning btn-sm" href="/auth/logout">
                            <i class="bi bi-box-arrow-right"></i> Logout
                        </a>
                    </div>
                <% } else { %>
                    <div class="d-flex">
                        <a class="btn btn-outline-light btn-sm me-2" href="/auth/google">
                            <i class="bi bi-google"></i> Google
                        </a>
                        <a class="btn btn-outline-light btn-sm" href="/auth/github">
                            <i class="bi bi-github"></i> GitHub
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</nav>

    <main class="container my-5">
        <div class="text-center">
            <h1 class="display-4 mb-4">🏏 India Cricket Team Statistics</h1>
            <p class="lead mb-5">Track and manage player statistics for the Indian cricket team</p>
            
            <% if (isAuthenticated && user) { %>
                <div class="alert alert-success mx-auto" style="max-width: 500px;">
                    <h4><i class="bi bi-check-circle-fill"></i> Successfully Logged In!</h4>
                    <p class="mb-0">Welcome, <strong><%= user.displayName || user.username || 'User' %></strong>!</p>
                    <small class="text-muted">Welcome to India Cricket Stats</small>
                </div>
                <a href="/players" class="btn btn-success btn-lg">
                    <i class="bi bi-speedometer2"></i> Go to Players Dashboard
                </a>
            <% } else { %>
                <div class="row justify-content-center">
                    <div class="col-md-6 col-lg-4">
                        <div class="card shadow">
                            <div class="card-header bg-primary text-white">
                                <h5 class="card-title mb-0">
                                    <i class="bi bi-lock-fill"></i> Login Required
                                </h5>
                            </div>
                            <div class="card-body">
                                <p class="card-text">Please login to access player statistics and management features.</p>
                                
                                <div class="d-grid gap-2">
                                    <a href="/auth/google" class="btn btn-danger btn-lg">
                                        <i class="bi bi-google"></i> Continue with Google
                                    </a>
                                    <a href="/auth/github" class="btn btn-dark btn-lg">
                                        <i class="bi bi-github"></i> Continue with GitHub
                                    </a>
                                </div>
                                
                                <div class="mt-3">
                                    <small class="text-muted">
                                        <i class="bi bi-info-circle"></i>
                                        Your data is secure and we only access basic profile information.
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="text-center py-4 mt-5">
        <div class="container">
            <p class="text-muted mb-0">
                &copy; 2025 India Cricket Team Statistics. 
                Built with <i class="bi bi-heart-fill text-danger"></i> for cricket fans.
            </p>
        </div>
    </footer>
</body>
</html>
