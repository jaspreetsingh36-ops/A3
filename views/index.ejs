<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🏏 India Cricket Stats</a>
            <div class="navbar-nav ms-auto">
                <% if (isAuthenticated && user) { %>
                    <span class="nav-text text-light me-3">Welcome, <%= user.displayName %></span>
                    <a class="nav-link" href="/players">Players</a>
                    <a class="nav-link" href="/auth/logout">Logout</a>
                <% } else { %>
                    <a class="nav-link" href="/auth/google">Login with Google</a>
                    <a class="nav-link" href="/auth/github">Login with GitHub</a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container my-5">
        <div class="jumbotron text-center">
            <h1 class="display-4">Welcome to India Cricket Team Statistics</h1>
            <p class="lead">Track and manage player statistics for the Indian cricket team</p>
            
            <% if (isAuthenticated && user) { %>
                <div class="alert alert-success">
                    <h4>Welcome back, <%= user.displayName %>!</h4>
                    <p>You are logged in with <%= user.googleId ? 'Google' : 'GitHub' %></p>
                </div>
                <a href="/players" class="btn btn-success btn-lg">View Players Dashboard</a>
            <% } else { %>
                <div class="row justify-content-center">
                    <div class="col-md-6">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">Login to Access Player Statistics</h5>
                                <p class="card-text">Choose your preferred login method:</p>
                                <a href="/auth/google" class="btn btn-danger btn-lg w-100 mb-3">
                                    <i class="fab fa-google"></i> Login with Google
                                </a>
                                <a href="/auth/github" class="btn btn-dark btn-lg w-100">
                                    <i class="fab fa-github"></i> Login with GitHub
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="text-center py-4">
        <div class="container">
            <p>&copy; 2024 India Cricket Team Statistics. All rights reserved.</p>
        </div>
    </footer>

    <!-- Add Font Awesome for icons -->
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
</body>
</html>